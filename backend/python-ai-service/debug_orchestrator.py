"""Debug script to test orchestrator initialization"""import osfrom dotenv import load_dotenvload_dotenv()print("🔍 DEBUGGING ORCHESTRATOR INITIALIZATION")print("=" * 50)gemini_key = os.getenv("GEMINI_API_KEY")openai_key = os.getenv("OPENAI_API_KEY")print(f"GEMINI_API_KEY found: {gemini_key is not None}")print(f"OPENAI_API_KEY found: {openai_key is not None}")if gemini_key:    print(f"GEMINI_API_KEY length: {len(gemini_key)}")if openai_key:    print(f"OPENAI_API_KEY length: {len(openai_key)}")try:    from orchestrator import create_study_assistant    print("✅ Orchestrator import successful")except ImportError as e:    print(f"❌ Orchestrator import failed: {e}")    exit(1)try:    api_key = gemini_key or openai_key or "demo-key"    orchestrator = create_study_assistant(api_key)    print("✅ Orchestrator initialization successful")except Exception as e:    print(f"❌ Orchestrator initialization failed: {e}")    import traceback    traceback.print_exc()